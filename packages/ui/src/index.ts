// @devdash/ui — public entry. The dashboard shell + tab-plugin API + viewer
// primitives land here across milestones M1–M4. M0 ships the contract version
// and the package skeleton only.

/** Wire-contract version negotiated with the backend (ADR-D12). */
export const DEVDASH_CONTRACT_VERSION = 1 as const

export type { } from './types'
