packages:
  - "packages/*"
  - "examples/*"
  - "standalone-ui"

# esbuild ships prebuilt platform binaries; allow its install script so pnpm
# does not flag it as an un-approved build (fatal in frozen installs).
allowBuilds:
  esbuild: true
